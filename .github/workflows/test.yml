name: Maven Package Release Build

on:
  repository_dispatch:
  push:
    branches:
      - 'master'
      - 'release*'

jobs:

  build-test-version-publish:
    name: Build Test Version Publish
    runs-on: ubuntu-18.04

    steps:
      - uses: actions/checkout@v1
      - uses: ZeroWasteTeam/action-read-base-version@master
